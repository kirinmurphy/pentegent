{
  "name": "@pentegent/scanner",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "npx tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "better-sqlite3": "^11.0.0",
    "ipaddr.js": "^2.2.0",
    "@pentegent/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/better-sqlite3": "^7.6.0",
    "vitest": "^2.0.0",
    "tsx": "^4.0.0"
  }
}
